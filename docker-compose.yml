version: '3.8'

services:
  myeventstream:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myeventstream
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${METRICS_PORT:-8080}:8080"
    # Healthcheck placeholder
    # healthcheck:
    #   test: ["CMD", "echo", "Healthcheck not implemented yet"]
    #   interval: 30s
    #   timeout: 3s
    #   start_period: 5s
    #   retries: 3
    # Note: Kafka/NATS setup will be added in later stages
